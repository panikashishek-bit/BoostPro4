// ─── Точка подключения пульта ────────────────────────────────────────────────
//
// Весь код пульта лежит в ./dashboard/ в корне репозитория: чтение настроек,
// проверка связи, счётчики и вёрстка страницы. Здесь — только адрес, по которому
// он открывается, и объявление «не кэшировать».
//
// Так сделано нарочно: пульт дорабатывается, не трогая приложение, и уносится
// в другой проект копированием одной папки. Новые счётчики и источники данных
// добавляются ВНУТРИ dashboard/ — этот файл при этом не меняется никогда.
//
// force-dynamic объявлен именно здесь, а не в dashboard/page.tsx: настройки
// маршрута Next читает из файла маршрута, и переэкспортированное значение
// может не заметить. Цена ошибки — молча закэшированная страница.

import DashboardPage from "../../../dashboard/page";

export const dynamic = "force-dynamic";
export default DashboardPage;
